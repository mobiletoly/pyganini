<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ metadata.title }}</title>
    <meta name="description" content="{{ metadata.description }}">
    <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 1 1'%3E%3C/svg%3E">
    <link rel="stylesheet" href="{{ layout.stylesheet_url }}">
    <script src="{{ layout.htmx_script_url }}"></script>
    <script src="{{ layout.sse_extension_script_url }}"></script>
    <script src="{{ layout.sse_helper_script_url }}"></script>
  </head>
  <body>
    <main class="page-shell">{{ child }}</main>
  </body>
</html>
