<section data-page="error" data-presentation="page">
  <p class="eyebrow">Request error {{ status_code }}</p>
  <h1>{{ title }}</h1>
  <p>This page is rendered through the selected route layout chain.</p>
</section>
